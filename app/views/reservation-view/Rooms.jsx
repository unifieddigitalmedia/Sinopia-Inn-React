const React = require('react');import Itemrow from './Itemrow.jsx';class Rooms extends React.Component {render() {var roomsArray = [];this.props.rooms.forEach((room,index) =>{roomsArray.push(<Itemrow item={room}  key={index} addtoArray={this.props.addtoArray} selectRoom={this.props.selectRoom} parent="rooms"/> ) ;}) ;return <ul className="demo-list-three mdl-list list"> 									{roomsArray} 		</ul>						}}module.exports = Rooms;