const React = require('react');class PlacesList extends React.Component {	constructor(props) {super(props);}render(){		var places = []; const context = this;this.props.places.forEach(function (element,index) {				//filterPlaces(e,element)							places.indexOf(element.businessname) === -1 ? places.push(<li onClick={(e) => context.props.drop(element) }  key={index}>  {element.businessname}  </li>) : "" ;			});return  <ul className="placeList">			{places}			 </ul>	}}module.exports = PlacesList;